# FPG2 BUILD FIX — TEST/INTEGRITY PASS

Naprawiono błędy z ostatniego GitHub Actions:
- synchronizacja Club.playerIds z Player.clubId przy tworzeniu GameEngine;
- TransferEngine synchronizuje rostery po transferach i zwrotach z wypożyczeń;
- SquadDepthEngine generuje pełny pool kandydatów, zamiast zbyt małej puli zależnej od fixed position order;
- poprawiono test MatchEngine (osobne składy gospodarzy i gości);
- poprawiono test transakcji meczu: commit odbywa się na rzeczywistym dniu fixture;
- poprawiono test rolloveru: zakończenie fixture przez playFixture zamiast obchodzenia date guard;
- poprawiono oczekiwaną formułę testu finansowego o penalty zależne od financialHealth;
- poprawiono test world lifecycle loan ownership;
- poprawiono nierealistyczny próg 20 meczów w 400 dniach (8 klubów = 14 meczów ligowych na sezon);
- LOAD może teraz przejść preflight dla poprawnego snapshotu, bo nowa kariera zapisuje spójne rostery.

Flutter/Dart SDK nie jest dostępny w tym środowisku, więc nie deklaruję lokalnego uruchomienia flutter test/analyze. ZIP jest przygotowany do ponownego uruchomienia CI.
Dodatkowo naprawiono błąd kompilacji w `GameEngine.advanceSimulationDay()`: metoda zwraca teraz `DailySimulationReport`, zgodnie z tym jak używa jej `career_home_screen.dart`. Wcześniej deklarowała `void`, przez co build kończył się błędami dla `seasonAdvanced` i `dateString`.
